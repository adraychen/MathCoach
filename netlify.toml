[build]
  command = "pip install -r requirements.txt"
  publish = "."

[build.environment]
  PYTHON_VERSION = "3.11"

[[redirects]]
  from = "/*"
  to = "/.netlify/builders/app"
  status = 200
  force = true

[functions]
  # We leave this empty to use default 2026 bundling settings
